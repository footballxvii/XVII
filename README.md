# XVII Stage 11B – Tactic Board Visibility Fix

This build keeps Stage 11 Formation Creator and adds a visibility fix.

## Fixes

- Formation Creator no longer stays locked after the transfer window.
- Stage 11 script now correctly uses the shared game state rather than looking only for `window.state`.
- Index now explicitly loads Stage 10F and Stage 11 scripts.
- Footer version updated to Version 11B · Beta.

Upload the full extracted folder contents to GitHub as normal.
